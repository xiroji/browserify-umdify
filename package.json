{
  "name": "vue-amdify",
  "version": "1.0.0",
  "description": "This module uses vue-component-compiler to construct a CommonJS module and then adds a bit of code so the component can be used in a AMD setting.",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "vue",
    "component",
    "amd"
  ],
  "author": "Hardeep Shoker",
  "license": "MIT",
  "dependencies": {
    "browser-unpack": "^1.0.1",
    "lodash": "^3.10.0"
  },
  "bin": {
    "vue-amdify": "bin/cmd.js"
  }
}
